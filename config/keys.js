const KEYS = {
  pageID: process.env.pageID,
  appID: process.env.appID,
  appSecret: process.env.appSecret,
  validationToken: process.env.validationToken,
  pageToken: process.env.pageToken,
};

module.exports = KEYS;
